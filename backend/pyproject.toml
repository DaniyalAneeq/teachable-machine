[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "python-multipart",
    "pillow",
    "torch",
    "torchvision",
    "scikit-learn",
    "joblib",
    "numpy",
]
